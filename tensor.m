function tensor
    scores = [
        95 83 76
        99 89 82
    ]
    scores_flat = scores(:)
    % scores_ndims = ndims(scores)
    % reshaped = reshape(scores, [1 fliplr(size(scores))])
    % reshaped_ndims = ndims(reshaped)
    % mean_by_student = mean(scores, 1)
    % mean_by_test = mean(scores, 2)
    % scores(2, 3, 1, 1, 1, 1)
    % ndims(mean_by_test)
    % cell_scores = {
    %     [95 83]
    %     [99 89 82]
    % }
end
